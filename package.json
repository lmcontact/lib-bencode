{
  "name": "lib-bencode",
  "version": "1.0.0",
  "description": "library to encode/decode bencoded string",
  "private": true,
  "devDependencies": {
    "@babel/parser": "^7.5.5",
    "@babel/types": "^7.5.5",
    "@types/jest": "^24.0.15",
    "@types/node": "^12.7.2",
    "jest": "^24.8.0",
    "prettier": "^1.18.2",
    "terser-webpack-plugin": "^1.3.0",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.3"
  },
  "dependencies": {},
  "scripts": {
    "test": "tsc && jest && yarn run clean",
    "clean": "rm -f src/*.js* tests/*.js*"
  }
}
